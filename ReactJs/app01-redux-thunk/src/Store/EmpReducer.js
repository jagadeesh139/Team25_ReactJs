const emp = []

const EmpReducers = (state = emp, action) => {
    switch (action.type) {
        case "CREATE":
            break;
        case "UPDATE":
            break;
        case "DELETE":
            break;
        default:
            return state

    }

}